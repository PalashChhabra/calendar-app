import { SplitDaysPipe } from './split-days.pipe';

describe('SplitDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
